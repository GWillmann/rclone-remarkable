#!/bin/bash

set -e
set -x

# Install rclone
# Create rclone nextcloud remote

# Create service (/etc/systemd/system/sync-nextcloud-to-remarkable.service)

# Create timer (/etc/systemd/system/sync-nextcloud-to-remarkable.timer )

# Create sync script (/usr/local/bin/ )

# Reload systemd daemons (systemctl daemon-reload)

# Start timer (systemctl start sync-nextcloud-to-remarkable.timer)

# View logs (journalctl -f)

# Remove all installation files